# Placeholder for local runtime secrets.
#
# Required for docker-compose.prod.yml:
# - ./secrets/audit-signing.key
#
# Expected format:
# - non-empty text key (recommended: 64 hex chars / 32 random bytes)
#
# Example:
#   openssl rand -hex 32 > secrets/audit-signing.key
#   chmod 600 secrets/audit-signing.key
